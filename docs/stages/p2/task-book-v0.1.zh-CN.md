# Zelyr Hypervisor — P2 阶段任务书 v0.1

**Translation status:** Current
**Translation source:** [English source](task-book-v0.1.md)
**Source blob:** `fff16cafdca4d2410c9e278d2c4e3c66e27b9057`
**Authority:** 本文是供中文读者使用的译文；[英文原文](task-book-v0.1.md)具有权威性。

**阶段 ID：** P2
**阶段名称：** 平台发现与 Host 内存基础
**状态：** 已定义的规划基线；不声明实施或验证完成。
**所有者／变更背景：** 从根目录源任务书重组 P2 规划。
**替代：** 根目录 `Rust Type-1 Hypervisor — P2 Stage Task Book v0.1.md` 的布局。
**主管文档：** [架构基线 ADR](../../adr/adr-000-architecture-baseline-v0.1.md)、
[文档索引](../../README.zh-CN.md)和 [Plan Agent 指南](../../development/plan-agent-guidelines.zh-CN.md)。

## 1. 目的与阶段边界

P2 将 P1 提供的稳定 AArch64 EL2 环境，转变为能够从启动信息推导可信 Host 平台事实、
并安全管理 Host 物理内存的系统。可观察的阶段成果是规范化的平台描述、规范化的启动物理内存图、
动态物理页与小对象分配，以及这些基础能力的诊断和验证。

P2 不运行 Guest。P3 使用其 CPU 清单、启动 CPU 关系、PSCI 事实、平台能力及分配基础开展 SMP 工作。
P4 使用其 Host RAM 拓扑、保留区排除、页分配及所有权统计基础开展 Stage-2 和 Validation Guest 工作。

### 必需

- 在解释传入的 AArch64 DTB 前验证它，包括活动 DTB 的位置／生命周期及畸形输入诊断；
- 解释 P2 所需的设备树（DT）语义，并安全地容忍可忽略的未知节点；
- 发现 CPU 清单、RAM、保留内存、GICv3 描述、timer、PSCI、`/chosen`／控制台事实及能力摘要；
- 将原始发现结果转换为按能力驱动的统一平台结果，使 Core 无需重新解析 DTB 或按板卡名称分支；
- 构建并规范化启动物理内存图，排除 Hypervisor、活动 DTB、固件／DT 保留区、启动制品、
  分配器元数据及其他已识别保留范围；
- 建立页分配／释放、OOM、多 RAM 区域、对齐、调试检查、统计，以及小对象动态分配基础；
- 使实际规范化结果可检查，提供离线 DTB 检查，并规划 Host／QEMU 负面和集成验证；
- 记录 P3／P4 移交、约束、已知限制及提出 P2 完成声明前所需证据。

### 预留

- 将页所有权扩展到 VM 拥有、共享、DMA 固定、COW、balloon／回收及相关后续阶段状态；
- 后续选择复制并释放原始 DTB、连续分配接口、分配器策略、元数据位置和分配器移交；
- 其他平台能力类型、设备发现及支持等级；
- Orange Pi 3B／RK3566 仅作为离线 DTB 兼容性样本。

### 范围外

- secondary CPU 启动、PSCI `CPU_ON`、SMP 锁、跨 CPU TLB shootdown 或任何 P3 执行机制；
- VM／vCPU 对象、VMID 分配、GuestAddressSpace、Guest 内存映射、Stage-2 转换、
  Guest EL1 入口或 Validation Guest 执行；
- GIC 初始化／IRQ 注入／vGIC、timer 虚拟化、PCI 发现或分配、SMMU／IOMMU、virtio、
  Control Domain、VM 配置、balloon、overcommit、swap／paging、snapshot／COW 或内存热插拔；
- Orange Pi 3B EL2 运行时启动或 Core 中针对板卡的行为。

P2 不得冻结解析器实现、crate／模块树、Rust 类型／API、分配器算法、元数据布局、
锁／并发设计或检查命令布局。这些选择需要后续获批准的详细设计。

## 2. 输入、约束与状态

P0 须提供已记录的 workspace／工具链、日志和 panic 基线、Host 测试与 QEMU runner 入口、
地址／错误约定、unsafe 治理及 ADR／文档流程。P1 须可证明已提供稳定的 QEMU `virt`
AArch64 EL2 Rust 入口、早期控制台及异常诊断、相关 CPU／EL2 寄存器、传入的 DTB 或等效启动信息、
已知的 Hypervisor 镜像物理范围和稳定的 Host 执行环境。缺少 P0／P1 输入是上游缺陷，
P2 不得绕过这些缺陷。

ADR 要求将 AArch64 DTB 发现结果转为 `PlatformInfo`，Core 避免依赖板卡／SoC／QEMU，
消费者按能力而非平台名称查询（ADR-041 至 ADR-045）。ADR 还要求受控 unsafe、
不可信边界上的 checked arithmetic、EL2 动态分配、以所有权为中心的内存演进、诊断及分层验证
（ADR-006、ADR-014、ADR-018、ADR-048、ADR-049）。

阶段完成时，预期的能力链为：

```text
启动 DTB -> 验证输入 -> 规范化平台事实 -> 规范化启动内存图
         -> 安全的可分配页池 -> 动态分配与诊断
```

这是必需成果图，**不**声称该能力链当前已经存在。

## 3. 架构变更记录

**P2-ACR-01 — ADR Required。** ADR 路线图中的 P2 条目要求定义最小
`MemoryObject`／`MemoryRegion` 结构；而 P2 源任务书又规定，P2 不得设计 P4 的对象系统，
只能预留扩展空间。按照已记录的权威顺序，这两个来源冲突。因此，P2 计划既不定义也不实施这些对象。
排期该项工作前，须有 ADR 澄清或替代的阶段要求。

## 4. 工作包图

| 工作包 | 必需成果与来源需求 | 主要验证 |
|---|---|---|
| P2-W01 | 已验证的启动平台描述输入：DTB 存在、范围、重叠、结构验证、核心 DT 编码语义及未知节点处理（P2-A01–A04）。 | P2-V01、P2-V02 |
| P2-W02 | 按能力驱动的发现及规范化平台事实：CPU、RAM、保留内存、GIC、timer、PSCI、console／chosen（P2-B01–B08、P2-C01–C03）。 | P2-V03、P2-V04 |
| P2-W03 | 规范化启动物理内存图及保留／所有权基础，包括检查后的范围和冲突处理（P2-D01–D08、P2-G01–G03）。 | P2-V05 |
| P2-W04 | 安全的 Host 物理页分配／释放、对齐、多区域支持、OOM、调试检查及统计（P2-E01–E08）。 | P2-V06 |
| P2-W05 | 动态小对象分配、明确的失败／释放行为及压力验证基础（P2-F01–F04）。 | P2-V07 |
| P2-W06 | 从实际平台／内存／分配结果导出的检查结果（P2-H01–H04）。 | P2-V08 |
| P2-W07 | 离线 DTB 兼容性检查与 QEMU／RK3566 样本覆盖（P2-I01–I05）。 | P2-V09 |
| P2-W08 | Host 侧畸形 DTB、内存图冲突、分配器及确定性回归覆盖（P2-J01–J05）。 | P2-V10 |
| P2-W09 | 跨 CPU 与 RAM 配置、统计和重复启动的 QEMU `virt` 集成覆盖（P2-K01–K05）。 | P2-V11 |
| P2-W10 | P2 平台／所有权契约、P3／P4 移交、已知限制及阶段门禁证据图（P2-L01–L05）。 | P2-V12、P2-V13 |

每个映射的工作包在 [plans/](plans/README.zh-CN.md) 中恰好有一个计划。
工作包计划只描述有界成果、顺序、验收与移交；它们不是详细设计、实施记录、验证证据或完成声明。

## 5. 依赖与执行图

```text
W01 -> W02 -> W03 -> W04 -> W05
  |      |      |      |      |
  |      +----> W06 <---------+
  |      +----> W07 -> W08 ---+
  +----------------------> W08 -> W09 -> W10
                 W06 ---------^
```

W06 消费 W02–W05 的稳定结果。W07 消费 W01–W02，并为 W08 提供样本／检查器证据。
W08 验证 W01–W05 和 W07 的 Host 侧属性。W09 在 W06／W08 后增加参考平台集成证据。
W10 在此前全部工作包之后记录消费者契约和证据图。依赖图有方向且无环。

## 6. 需求到验证的可追溯性

| 需求组 | 验证 ID | 通过条件 |
|---|---|---|
| P2-A01–A04 | P2-V01、P2-V02、P2-V10 | 输入可用性／范围错误和畸形结构被拒绝，给出局部诊断；解释有效的必需编码时没有不安全的越界行为。 |
| P2-B01–B08、P2-C01–C03 | P2-V03、P2-V04、P2-V09、P2-V11 | 参考与样本输入稳定地产生 CPU／RAM／保留／GIC／timer／PSCI／console 事实及能力状态，Core 不按板卡名选择。 |
| P2-D01–D08、P2-G01–G03 | P2-V05、P2-V10、P2-V11 | 内存图有序且规范化，对溢出／冲突进行检查，排除每个受保护范围，并预留未来所有权扩展。 |
| P2-E01–E08 | P2-V06、P2-V10、P2-V11 | 跨支持区域分配／释放页；OOM 和无效操作明确；绝不返回受保护页，统计保持一致。 |
| P2-F01–F04 | P2-V07、P2-V10 | 小对象分配有明确的失败／释放行为，压力运行保持分配／统计不变量。 |
| P2-H01–H04 | P2-V08、P2-V11 | 检查报告实际规范化结果及分配器统计，而非另一个硬编码视图。 |
| P2-I01–I05 | P2-V09、P2-V10 | 离线检查器报告 QEMU／RK3566 样本的 P2 就绪情况，对不支持的无关设备发出警告，但不声称板卡运行时支持。 |
| P2-J01–J05 | P2-V10 | 所列负面、耗尽、压力、冲突和确定性案例有可重复的 Host 侧证据。 |
| P2-K01–K05 | P2-V11 | QEMU `virt` 结果在必需的 CPU／RAM 变体与重复启动中保持稳定；内存图统计处于已记录的预期范围。 |
| P2-L01–L05 | P2-V12、P2-V13 | 消费者契约、已知限制、证据位置及所有阶段门禁可审查，且不声称实施完成。 |

## 7. 阶段验证矩阵

| ID | 所需证据 | 成功条件 |
|---|---|---|
| P2-V01 | 启动输入边界评审／测试 | DTB 缺失、位置、长度、Host 可访问范围及危险的 Hypervisor 重叠产生明确结果。 |
| P2-V02 | DTB 结构／编码测试 | magic、总大小、块、偏移、属性大小、cell 和引用均留在已验证边界内；受损输入不导致失控失败。 |
| P2-V03 | 发现结果测试 | 声明存在时，收集必需的 CPU、RAM、保留、GIC、timer、PSCI 和 chosen／console 事实。 |
| P2-V04 | 规范化／可移植性评审 | 消费者使用规范化事实／能力；缺失、不支持、不存在及可用状态保持可区分；Core 不按板卡名称分支。 |
| P2-V05 | 启动内存图测试 | RAM 与所有受保护／已拥有区域进行有序、检查过且无歧义的规范化处理，包括空洞和冲突。 |
| P2-V06 | 页分配测试 | 分配／释放／对齐／多区域／OOM／调试／统计行为明确，不返回受保护页。 |
| P2-V07 | 小对象分配压力证据 | 重复分配／释放、耗尽和恢复维持所声明的分配不变量。 |
| P2-V08 | 检查功能评审 | 平台摘要、内存图转储及分配器统计来自活动的规范化状态。 |
| P2-V09 | 离线检查器样本证据 | QEMU `virt` 和 Orange Pi 3B／RK3566 DTB 获得客观就绪报告；不声称 Orange Pi EL2 运行时支持。 |
| P2-V10 | 负面／属性回归 | 畸形 DTB、内存图冲突／溢出、分配器耗尽／压力及重复发现的确定性符合各工作包验收条件。 |
| P2-V11 | QEMU 集成证据 | 必需的 QEMU CPU 数／RAM 大小配置和重复启动符合发现、内存图统计及检查功能预期。 |
| P2-V12 | P3／P4 移交评审 | 消费者可找到支持的输入、限制与证据位置，且不推断 P2 的实施设计。 |
| P2-V13 | 阶段治理评审 | 每个必需工作包对应一个计划，每个验证 ID 有条件，链接／依赖可解析，P2-ACR-01 保持可见。 |

规划该矩阵不提供证据。命令、日志、环境和实际结果属于 [verification/](verification/)；
实施可追溯记录属于 [implementation/](implementation/)。

## 8. 退出条件与移交

只有 P2-V01 至 P2-V13 均有证据，且满足以下全部条件，P2 才能标为完成：

1. QEMU `virt` 可靠地产生必需平台事实及规范化表示，下游 Core 消费者不通过重新解析 DTB 获取这些信息。
2. 在每个有效的分配／释放序列中，每个受保护物理范围均被排除在分配之外；这是 P2 严格的安全门禁。
3. 页和小对象分配的正常、耗尽及释放行为明确，并具有可观察的统计。
4. 平台摘要、内存图转储和分配器统计反映活动的规范化状态。
5. 存在 Host 侧负面回归与 QEMU 集成证据；离线 QEMU／RK3566 样本检查展示语义可移植性，
   但不声称板卡运行时支持。
6. P3、P4 可以找到移交契约和证据，包括已知限制与 P2-ACR-01。

P3 移交仅暴露 CPU 清单、启动 CPU 关系、PSCI／能力事实及可用分配能力；不授权 AP 启动。
P4 移交仅暴露 Host RAM 拓扑、受保护范围排除、分配／释放及所有权统计扩展基础；
不授权 Stage-2、VM 或 Guest 工作。已知限制至少须明确列出 PCI 发现、SMMU／IOMMU、
GIC 初始化、AP 启动及 Orange Pi 3B 运行时支持属于后续工作。

## 9. 完成评审

提出完成声明前，用以下问题检查验证记录、保持的边界、依赖图与移交：

- 消费者能否区分可信的已发现事实与不支持或不可用的能力提示？
- 是否能证明任何分配路径都排除了全部受保护页？
- 畸形启动输入和范围运算是否按不可信输入处理？
- 是否有 P3／P4 或针对板卡的运行时机制进入 P2？
- P2-ACR-01 是否由授权来源解决，或保留为开放的架构问题？

任一答案不符合要求，都阻止 P2 完成。
